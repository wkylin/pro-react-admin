# 高级示例

此示例包含图片、表格和更多代码块。

![替代文本](https://via.placeholder.com/300x120.png?text=MDX+Image)

```ts
type User = {
  id: number
  name: string
}

const user: User = { id: 1, name: 'Alice' }
```

- [ ] 任务 A
  - 嵌套任务 a1
- [x] 任务 B

---

更多内容，可在编辑器中直接修改并下载为 `.mdx` 文件。