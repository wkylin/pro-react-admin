import React from 'react'
import FixTabPanel from '@stateless/FixTabPanel'

const WebpackList = () => {
  return (
    <FixTabPanel>
      <div style={{ padding: 24 }}>
        <h2>Webpack 构建工具示例</h2>
        <p>这是一个自动生成的测试组件。</p>
      </div>
    </FixTabPanel>
  )
}

export default WebpackList
